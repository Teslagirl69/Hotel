<%= form_with(model: review) do |form| %>
  <% if review.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
        <% review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label "enter your name", style: "display: block" %>
    <%= form.search_field :author_name %>
  </div>

  <div>
    <%= form.label "enter your email", style: "display: block" %>
    <%= form.email_field :author_email %>
  </div>

  <div>
    <%= form.label "enter the text of your review", style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
