<p style="color: green"><%= notice %></p>
<%= render 'layouts/navigation' %>

<h1>Rooms</h1>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
    <p>
      <%= link_to "Show this room", room %>
    </p>
  <% end %>

</div>
